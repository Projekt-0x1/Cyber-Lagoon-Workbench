# Foundry workshop -> Direct portability matrix

Status: reference-only boundary inventory. Physical Direct parity was not run for this workbench and remains RED.

## Retained historical records and current surfaces

| Retained artifact | Production surface | Status | Scope |
|---|---|---|---|
| `language_direct_relseq_gpu_receipt.json` | `direct_relational_sequence_composition.cuh` | UNVERIFIED HISTORICAL DECLARATION; STALE SOURCES REMOVED | Its former reference learner/builder inputs were deleted. The artifact and sanitizer log were not verified here; it is not current parity evidence. |
| `direct_adult_relseq_bridge_gpu_receipt.json` | `direct_adult_relational_sequence_bridge.cuh` + Recipe/Occurrence-shaped identities | UNVERIFIED HISTORICAL DECLARATION; DIRECT SOURCE HASHES MATCH | Three direct file hashes match. The artifact, sanitizer log, toolchain, build command, transitive sources, scheduler and whole-state execution were not verified here. |
| `direct_whitebox_recipe_materialization.cuh` | resident white-box Recipe materialization/replay | PRODUCTION DONOR | Generic materialization law exists; broader Foundry IR translation is not defined. |
| `direct_foundry_condensation_workspace.cuh` | Adult named-parent whitebox mint | HOST+DEVICE JOIN / C0_RED | Transaction around `materialize_resident_whitebox_recipe`. Optional N gate calls Adult `admit_resident_network_parent`. Not a second mind. |
| `direct_adult_resident_relational_network.cuh` | Adult N / B_N / coactive set | PRODUCTION | Closure + `admit_resident_network_parent` + `bind_resident_relational_network_set`. Two construction Ns stay two Ns. Whole-frontier-as-one-N refuses. Eligibility is N geometry, not a neuron count. Recruited credit lands on one construction; a second N that shares B_N stays at 0. Eligibility refresh keeps that credit. Withdrawing the alternate still selects the credited source; withdrawing the source leaves no select winner and does not erase stored credit. Equal credit on both constructions leaves select unresolved; one extra unit on the source breaks the tie. Recruitment identity now folds coupling phrase variables, so the same Recipes on a new surface are a new rid at credit 0; the old surface keeps its credited rid. A later pair of the two minted Recipes is another new recruitment with zero credit; fragments do not inherit the source construction's credit. Two expresses of the condensed Recipe on one surface do not couple or observe as a second world contact. After those expresses, the source pair still observes; the original surface still selects with stored credit. Two expresses of one Recipe on a chained surface form a new N with a new rid; they do not observe as the source construction. Motor pressure counts a whole recruited construction; a mixed-member fragment contributes 0. Descriptor lives in `direct_adult_participation_descriptor.cuh`; isolation proves the same law without `direct_adult_core.cuh`. |
| `direct_adult_resident_network_whitebox_observe.cuh` | mixed-rank N → tagged algebra evidence → dispatch | HOST+DEVICE / C0_RED | Evidence writes `direct_relation_algebra_resident_word(linear)`, not raw 0. Cell `credit_q16` and Occurrence eligibility are not in the witness. Eligibility still changes N identity. Minted p(n+1) rebinds on a held-out surface, forms a later N with a p(0) ancestor, and a later linear+linear N observes/replays a new candidate. Same construction on a new surface replays the same logical. Pairing that evidence with a different construction's witness refuses: replay requires the offered whitebox to be the exact 2-source fold of the evidenced members. Two contacts of that construction are different evidence closures; the minted Recipe binds the new surface directly. A 3-linear path can fold a whitebox witness; 2-source evidence still refuses. That fold is not the minted 2-source `p(n+2)` Recipe: different witness, different logical, whitebox replay against the minted cell refuses, and the 2-source evidence ticket cannot replay the longer fold. The same construction rebound as endogenous (authority none) is a reconstructive Network with `actual_count=0`: same recruitment identity as the heard surface (compact morphology, not a tape), different transient N identity, observe refuses as world contact, independent-external authority cannot be faked onto it, and heard+imagined stay two coactive Ns. Settling the heard Occurrences refuses that transient N and refuses re-observe; the Recipes stay; condensation evidence still replays the minted logical; a later endogenous bind and a later actual utterance both unfold the same rid. N admission now requires every member to be actual, so an imagined closure cannot gate mint even when the parent Recipe is a member; a later heard utterance of the same construction still can. A language construction and a non-language construction can share a causal intersection node (not a letter or image file): the connected frontier is one N with reconvergence, affine fold refuses, and the two formal couplings remain two Ns. An endogenous language Occurrence can intersect the same world member as-if: the mixed N has `actual_count=1`, cannot admit, and cannot fold. Two reconstructive cues can share that same world node as two Ns; a transient three-member coalition may form and still cannot admit or fold. There is no stored Alice record. Affine fold now refuses a 4-linear path (`C=4`) while a 3-linear path still folds; 2-source evidence remains the mint ticket. Depth is capped in Adult mathematics, not by an English parser. |
| `direct_foundry_phase_lower.cuh` | 1610 phase *names* on RelSeq + Adult mint | HOST JOIN / C0_RED | Not the Adult loop. Not IR translation. CONDENSE rebinds N then mints. |
| `cuda_foundry_parent_isolation_contract.cu` | device `foundry_materialize_from_workspace` | SEAM DIFFERENTIAL / not whole-Adult | Same helper as phase_lower. Matching episode stamps the parent; foreign does not. Mismatched parent_logical refuses. No Adult include. Post-`p(n+2)` tail is `language_after_pn2.inl`, shared with host; isolation proves the same B_N ports. |
| `cuda_foundry_whitebox_condense_contract.cu` | device materialize/replay of whitebox p(n+1) | SEAM DIFFERENTIAL / not whole-Adult | Mints through `foundry_materialize_from_workspace`. Duplicate witness refuses. |
| `cuda_foundry_mixed_rank_network_contract.cu` | device `bind_resident_relational_network_closure` | SEAM DIFFERENTIAL / not whole-Adult | Mixed-rank N identity matches host; B_N is unique uncoupled ports; coupling 40 exposes 50; uncoupled shared variable refuses. |
| `cuda_foundry_bn_candidate_contract.cu` | device B_N admission + whitebox p(n+1) | SEAM DIFFERENTIAL / not whole-Adult | Rebind then mint through `foundry_materialize_from_workspace`. Frontier and bank stay separate. |
| `direct_adult_resident_executable_morphology.cuh` | generic route/morphology lowering | PRODUCTION DONOR | Sparse/compiled execution work classes exist; reference-contract coverage is incomplete. |
| `direct_dense_tensor_execution.cu` | WMMA/Tensor Core dense lowering | HARDWARE SURFACE PRESENT | Acceleration surface only; no semantic authority. |
| `bcc32_grown_selective_state_space.cuh` | state-space temporal lowering | HARDWARE SURFACE PRESENT | Acceleration/execution hypothesis only. |
| `bcc32_cuda_paged_resident_route_bank.cuh` | sparse paged route storage | HARDWARE SURFACE PRESENT | Candidate quantity/locality substrate. |
| RT/BVH/OptiX | none found in current sparse source inventory | NOT IMPLEMENTED | Optional lowering to earn by parity/resource measurement, not doctrine. |

## Promotion rule

A workshop mechanism becomes physical Adult machinery only after a generic mapping exists and same-state/same-contact differential tests match protected reference transitions. If the existing Direct substrate cannot express a useful mechanism cleanly, improve the generic substrate once rather than compiling a semantic kernel per Recipe.

The boundary verifier compiles and runs the current host structural bridge, classifies both historical JSON records as unverified declarations, and reports `physical_direct_parity=NOT_RUN/RED`. No row in this file is a language-capability or AGI receipt.
